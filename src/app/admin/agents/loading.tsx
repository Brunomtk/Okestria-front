import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Operations" title="Loading agents" subtitle="Across every tenant — name, role, profile." />;
}
