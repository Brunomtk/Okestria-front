import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Infrastructure" title="Loading database" subtitle="Tables + row counts + S3 buckets." />;
}
