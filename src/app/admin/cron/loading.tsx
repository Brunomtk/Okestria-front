import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Operations · Cron" title="Loading scheduled jobs" subtitle="All cron schedules across the platform." />;
}
