import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Tenants" title="Loading workspaces" subtitle="Office layouts + agents + squads." />;
}
