import { AdminLoading } from "../_components/AdminLoading";

export default function Loading() {
  return (
    <AdminLoading
      eyebrow="Tenants"
      title="Loading companies"
      subtitle="Fan-out across companies + users + agents + workspaces."
    />
  );
}
