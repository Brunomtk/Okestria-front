import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Operations · Leads __EYEBROW__ Missions" title="Loading missions" subtitle="Squad executions running across tenants." />;
}
