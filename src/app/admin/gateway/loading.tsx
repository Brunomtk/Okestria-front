import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Infrastructure" title="Loading gateway" subtitle="OpenClaw runtime + tokens + dispatches." />;
}
