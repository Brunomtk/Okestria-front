import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Finance" title="Loading billing" subtitle="Plans, subscriptions and usage rolled up per tenant." />;
}
