import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Pipeline" title="Loading leads" subtitle="Prospects + insights + outreach." />;
}
