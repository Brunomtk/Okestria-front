import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Tenants" title="Loading users" subtitle="Provisioned across every tenant." />;
}
