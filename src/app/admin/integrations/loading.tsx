import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Infrastructure" title="Loading integrations" subtitle="Apify, Resend, Stripe and more." />;
}
