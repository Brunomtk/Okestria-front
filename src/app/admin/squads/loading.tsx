import { AdminLoading } from "../_components/AdminLoading";
export default function Loading() {
  return <AdminLoading eyebrow="Operations" title="Loading squads" subtitle="Multi-agent missions across tenants." />;
}
